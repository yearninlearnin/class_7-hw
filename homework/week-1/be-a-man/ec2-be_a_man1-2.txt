#!/bin/bash
# Thank you Theo先生 and Admins for the script!!!!!!
# Use this for your user data (script from top to bottom)
# install httpd (Linux 2 version)
yum update -y
yum install -y httpd
systemctl start httpd
systemctl enable httpd


#Class7 - HW - BE A MAN CHALLENGE 1.2
    # Megumi is waiting ..honto ni

# Get the IMDSv2 token
TOKEN=$(curl -X PUT "http://169.254.169.254/latest/api/token" -H "X-aws-ec2-metadata-token-ttl-seconds: 21600")

# Background the curl requests
curl -H "X-aws-ec2-metadata-token: $TOKEN" -s http://169.254.169.254/latest/meta-data/local-ipv4 &> /tmp/local_ipv4 &
curl -H "X-aws-ec2-metadata-token: $TOKEN" -s http://169.254.169.254/latest/meta-data/placement/availability-zone &> /tmp/az &
curl -H "X-aws-ec2-metadata-token: $TOKEN" -s http://169.254.169.254/latest/meta-data/network/interfaces/macs/ &> /tmp/macid &
wait

macid=$(cat /tmp/macid)
local_ipv4=$(cat /tmp/local_ipv4)
az=$(cat /tmp/az)
vpc=$(curl -H "X-aws-ec2-metadata-token: $TOKEN" -s http://169.254.169.254/latest/meta-data/network/interfaces/macs/${macid}/vpc-id)

echo "
<!doctype html>
<html lang=\"en\" class=\"h-100\">
<head>
    <title>Details for EC2 instance</title>
<style>
    body {
        background-image: url('https://i.postimg.cc/h4LRD8Yw/Cyberpunk-Dark-Neon-Japanese-Style-Apartments-AI-Artwork-5-1-1024x574.jpg');
        background-size: cover;
        background-position: center;
        background-repeat: no-repeat;
        background-attachment: fixed;
        margin: 0;
        padding: 0;
        min-height: 100vh;
        display: ;
        flex-direction: column;
        justify-content: center;
        align-items: center;
    }
    
    div {
        text-align: center;
        padding: 30px;
    }
    
    img {
        display: block;
        margin: 0 auto 10px auto;
    }
    
    h1, p {
        color: #ff1493;
        text-shadow: 2px 2px 4px rgba(0,0,0,0.9);
    }
  
</style>
  
</head>
<body>
    <div>
        <h1>なんでまだ「ロードバランス」されてないの?。。。</h1>
        <h1> 「宿題のチャレンジ1.2」 </h1>　
      
      　<h1>"I found my wife at a small meetup in Fukuoka! Her name is Megumi!"</h1>
        <br>
      
      <img src="https://i.postimg.cc/zfq90p1W/rinah-cropped.jpg">
      <h1>インフォメーション👇🏾</h1>
        <p><b>インスタンス名:</b> $(hostname -f) </p>
        <p><b>インスタンスのプライベートIPアドレス: </b> ${local_ipv4}</p>
        <p><b>アベイラビリティ ゾーン: </b> ${az}</p>
        <p><b>仮想プライベートクラウド (VPC):</b> ${vpc}</p>
    </div>
</body>
</html>
" > /var/www/html/index.html

# Clean up the temp files
rm -f /tmp/local_ipv4 /tmp/az /tmp/macid